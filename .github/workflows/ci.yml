name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  lint_and_test:
    runs-on: ubuntu-latest
    if: false # Template placeholder; enable after defining stack/quality gates.
    steps:
      - name: Placeholder
        run: echo "CI disabled in template"

      # - uses: actions/checkout@v4

      # - name: Set up Node.js
      #   uses: actions/setup-node@v4
      #   with:
      #     node-version: 20
      #     cache: pnpm

      # - name: Enable pnpm via corepack
      #   run: corepack enable pnpm

      # - name: Install dependencies
      #   run: pnpm install --frozen-lockfile

      # - name: Lint
      #   run: pnpm lint

      # - name: Format (check)
      #   run: pnpm format:check

      # - name: Test
      #   run: pnpm test

      # - name: E2E
      #   run: pnpm test:e2e
